[project]
name = "dags"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "apache-airflow==2.10.2",
    "apache-airflow-providers-postgres",
    # "pandas",
    "pandas>=3.0.0",
    "psycopg2-binary>=2.9.11",
    "requests",
    "sqlalchemy",
]
